#include <stdio.h>
#include <stdlib.h>
#include <memory.h>
#pragma warning(disable:4996)
static int length = 20;


void print_arr(int*);
void swap_int(int*, int*);
void swap_index(int, int, int*);
int* Interchange_sort(int*);
int* Selection_sort(int*);
int* Bubble_sort(int*);
int* Insertion_sort(int*);
int* Shell_sort(int*);
int* Heap_sort(int*);
int* Quick_sort(int*);
void Merge_sort(int*, int, int);
